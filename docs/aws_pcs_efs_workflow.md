# AWS PCS + EFS Workflow

This document is superseded by:

```text
docs/aws_parallelcluster_workflow.md
```

AWS PCS was replaced for this iteration because the lab prefers ParallelCluster's
lower standing cost profile when compute resources are idle.
